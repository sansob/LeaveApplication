<UserControl x:Class="LeaveApplication.UserInterfaceLeaveApp.UserControl.HistoryControl"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="1000">
    <Grid Background="#FFFFFF">
        <Rectangle Fill="#FFECF0F1" HorizontalAlignment="Left" Height="122" VerticalAlignment="Top" Width="1033" Margin="0,0,-33,0"/>
        <TextBlock HorizontalAlignment="Left" Margin="31,26,0,0" TextWrapping="Wrap" Text="Leave Request History" VerticalAlignment="Top" Width="312" Height="46" FontFamily="Segoe UI" FontSize="36"/>
        <TextBlock HorizontalAlignment="Left" Margin="31,77,0,0" TextWrapping="Wrap" Text="View Leave Request History" VerticalAlignment="Top" Width="575" FontSize="20" FontFamily="Lato Light"/>

        <DataGrid HorizontalAlignment="Left" Height="230" Margin="10,210,0,0" VerticalAlignment="Top" Width="980" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="No" Binding="{Binding Path=No}"></DataGridTextColumn>
                <DataGridTextColumn Header="Requester" Binding="{Binding Path=Date}"></DataGridTextColumn>
                <DataGridTextColumn Header="Leave start date" Binding="{Binding Path=StartTime}"></DataGridTextColumn>
                <DataGridTextColumn Header="Leave end date" Binding="{Binding Path=EndTime}"></DataGridTextColumn>
                <DataGridTextColumn Header="Leave duration" Binding="{Binding Path=OvertimeType}"></DataGridTextColumn>
                <DataGridTextColumn Header="Create Date" Binding="{Binding Path=Activity}"></DataGridTextColumn>
                <DataGridTextColumn Header="Action" Binding="{Binding Path=Action}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Request Leave" HorizontalAlignment="Left" Margin="868,151,0,0" VerticalAlignment="Top" Width="auto" Click="Button_Click"/>

    </Grid>

</UserControl>
